# Project Moon Review — Design Intent

Analyze the persisted user request, pinned branch/commit metadata, changed files, commit history when useful, and the pinned diff. Produce a design-intent document that states the problem, decisions, alternatives/tradeoffs, intentionally excluded scope, and assumptions that reviewers must respect. Surface ambiguity instead of inventing missing intent.
